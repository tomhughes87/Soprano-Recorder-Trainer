# Zelda song references

Each child folder owns one playable input sequence and any verified longer continuation. The current data was migrated from the original flat `zeldaShort.ts` and `zeldaLong.ts` files; no external score files or authoritative source URLs were documented there.

Add a `reference.md`, score image, MIDI, or MusicXML file inside an individual song folder when that song is verified.
